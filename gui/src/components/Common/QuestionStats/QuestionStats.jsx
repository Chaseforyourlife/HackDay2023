import React from 'react';
import TimeStamp from "../TimeStamp/TimeStamp"

const QuestionStats = () => {
    return (
        <div>
            <TimeStamp />
        </div>
    );
}

export default QuestionStats;
